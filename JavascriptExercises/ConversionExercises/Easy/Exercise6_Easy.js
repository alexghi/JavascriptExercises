//remove all zeros and negative numbers from the 2d array
const exercise6_easy = {
  initialForm: [[1, 0, 1, 0], [0, -3, 0, 0], [1, -2, 20], [1, 44, 2], [-22]],
  expectedForm: [[1, 1], [], [1, 20], [1, 44, 2], []],

  //You must convert initialForm object to be the same as expectedForm
  //Use this function
  execute: function (initialForm) {
    const result = [];
    // conversion code here
    return result;
  },
};

addCategory(categories.conversion_easy, exercise6_easy, "Exercise 6 Easy");
