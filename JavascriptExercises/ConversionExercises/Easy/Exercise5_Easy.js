//Convert the 2D array into an 1D array that contains the sum of elements of each inner array
const exercise5_easy = {
  initialForm: [
    [1, 2, 3, 4],
    [2, 3, 4, 5],
    [5, 5, 5],
  ],
  expectedForm: [10, 14, 15],

  //You must convert initialForm object to be the same as expectedForm
  //Use this function
  execute: function (initialForm) {
    const result = [];
    // conversion code here
    return result;
  },
};

addCategory(categories.conversion_easy, exercise5_easy, "Exercise 5 Easy");
